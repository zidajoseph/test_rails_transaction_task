<h1>受注一覧</h1>

<table border=1>
  <thead>
    <tr>
      <td>商品名</td><td>注文数</td>
    </tr>
  </thead>
  <tbody>
  <% @items.each do |item| %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.total_quantity %></td>
    </tr>
  <% end %>
  </tbody>
</table>
